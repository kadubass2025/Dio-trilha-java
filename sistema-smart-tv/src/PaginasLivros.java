import java.util.Scanner;

public class PaginasLivros {
  public static void main(String[] args) {
    Scanner scanner = new Scanner(System.in);

    // 1. Entrada dos minutos
    System.out.println("\nDigite os minutos: ");
    int minutos = scanner.nextInt();
    // 2. Entrada dos dias
    System.out.println("\nDigite os dias: ");
    int dias = scanner.nextInt();
    // 3. Calcular economia por dia
    System.out.println("A econimia por dia eh: " + minutos*12);

  if (condition:var(boolean)) {
    
  } else {
    
  }
    }
  // 4. Calcular economia total
  // 5. Exibir resultado
}}